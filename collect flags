#include<stdio.h>
int main()
{
int a,b;
printf("Enter the aximum number of times\n");
scanf("%d",&a);
printf("Enter the number games played\n");
scanf("%d",&b);
if(b<a)
{
printf("In\n");
}
else if(b>=a)
{
printf("OUT\n");
}
return 0;
}
